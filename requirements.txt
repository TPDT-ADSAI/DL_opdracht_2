# RunPod: gebruik PyTorch image (torch + CUDA pre-installed).
# Installeer deze lijst met:  pip install -r requirements.txt
# (torch wordt NIET gepind — gebruik wat de image levert)

# --- core data / numeriek ---
numpy<2.0
pandas>=2.0,<2.3
scipy>=1.11
scikit-learn>=1.4,<1.6

# --- plotting ---
matplotlib>=3.7
seaborn>=0.13
pydot>=2.0           # voor keras.utils.plot_model
wordcloud>=1.9

# --- beeld / audio ---
Pillow>=10.0
opencv-python-headless>=4.8   # headless = geen GUI libs, kleiner op RunPod
librosa>=0.10

# --- deep learning ---
tensorflow==2.18.0   # gebruikt torch's CUDA 12.8 libs via LD_LIBRARY_PATH shim in cel 0
transformers>=4.40,<4.50
datasets>=2.18,<3.0
evaluate>=0.4
huggingface_hub>=0.23,<1.0

# --- hyperparam search ---
optuna>=3.5

# --- notebook runtime ---
ipykernel>=6.29
ipython>=8.20
tqdm>=4.66
